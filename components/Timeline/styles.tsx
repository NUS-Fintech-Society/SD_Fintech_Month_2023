const timelineWrapper = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  height: '360vh', // Adjust the height as needed
};

const timelineBox = {
  backgroundColor: 'white',
  borderRadius: '20px',
  boxShadow: '0 4px 8px rgba(0, 0, 0, 0.1)',
  overflow: 'hidden',
  width: '950px', // Adjust the width as needed
  height: '2400px', // Adjust the height as needed
};

const timelineTitle = {
  color: '#0c1747', // Use camelCase instead of hyphenated font-size
};

export { timelineWrapper, timelineBox, timelineTitle };
